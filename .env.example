# PropelAI v5.0 Environment Configuration
# Copy this file to .env and fill in your values

# ===========================================
# Environment
# ===========================================
PROPELAI_ENV=development  # development, staging, production

# ===========================================
# API Server
# ===========================================
API_HOST=0.0.0.0
API_PORT=8000

# ===========================================
# Database (PostgreSQL + pgvector)
# ===========================================
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=propelai
POSTGRES_USER=propelai
POSTGRES_PASSWORD=your-secure-password-here

# Full connection string (overrides individual settings)
# DATABASE_URL=postgresql+asyncpg://propelai:password@localhost:5432/propelai

# ===========================================
# Authentication
# ===========================================
JWT_SECRET=your-jwt-secret-change-in-production
JWT_EXPIRATION_HOURS=24

# Development mode bypass (auto-generates dev user if no token)
DEV_TENANT_ID=00000000-0000-0000-0000-000000000001
DEV_USER_ID=00000000-0000-0000-0000-000000000001

# OAuth2 Provider (future - Auth0/Supabase)
# AUTH0_DOMAIN=your-tenant.auth0.com
# AUTH0_CLIENT_ID=your-client-id
# AUTH0_CLIENT_SECRET=your-client-secret

# ===========================================
# Storage
# ===========================================
STORAGE_BACKEND=local  # local, s3

# Local storage path (for development)
STORAGE_PATH=./data/storage

# S3/MinIO Configuration (for production)
# S3_BUCKET_NAME=propelai-storage
# S3_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# S3_ENDPOINT_URL=http://localhost:9000  # For MinIO

# ===========================================
# LLM Providers
# ===========================================
# Google Gemini (Primary)
GOOGLE_API_KEY=your-google-api-key
GOOGLE_PROJECT_ID=your-project-id

# Anthropic Claude (Alternative)
ANTHROPIC_API_KEY=your-anthropic-api-key

# OpenAI (Fallback)
OPENAI_API_KEY=your-openai-api-key

# ===========================================
# Vector Store
# ===========================================
# Using pgvector (built into PostgreSQL)
# No additional configuration needed

# Pinecone (optional, for production scale)
# PINECONE_API_KEY=your-pinecone-key
# PINECONE_ENVIRONMENT=us-east-1
# PINECONE_INDEX_NAME=propelai-proposals

# ===========================================
# Feature Flags
# ===========================================
ENABLE_OCR=true
ENABLE_AUDIT_LOG=true
ENABLE_COMPETITOR_GHOSTING=false

# ===========================================
# Logging
# ===========================================
LOG_LEVEL=INFO
DB_ECHO=false  # Set to true for SQL query logging
