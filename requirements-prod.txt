# PropelAI - Production Dependencies

# API Framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6
pydantic>=2.0.0
pydantic-settings>=2.0.0
email-validator>=2.0.0

# Security & Authentication
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0
cryptography>=41.0.0
pyotp>=2.9.0

# Document Processing
pypdf>=3.0.0
pdfplumber>=0.10.0  # v4.0 Phase 1: Coordinate extraction for Trust Gate
python-docx>=0.8.11
openpyxl>=3.0.10

# Database (v4.0 Phase 4: PostgreSQL + pgvector)
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.0
pgvector>=0.2.0  # pgvector Python client for vector similarity search

# AI/ML (v4.0 Phase 4: Embeddings for semantic search)
openai>=1.0.0  # For text-embedding-3-small embeddings
# voyageai>=0.2.0  # Optional: Anthropic's recommended embedding provider

# HTTP Client
httpx>=0.24.0
aiohttp>=3.8.0

# Caching (optional)
redis>=4.5.0

# Email
sendgrid>=6.10.0
aiosmtplib>=3.0.0

# Utilities
python-dateutil>=2.8.0
orjson>=3.9.0
